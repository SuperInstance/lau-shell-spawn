[package]
name = "lau-shell-spawn"
version = "0.1.0"
edition = "2021"
description = "Shell spawning system — Hermes creates child shells in their own sandboxes"
license = "MIT"
repository = "https://github.com/SuperInstance/lau-shell-spawn"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0.150"
